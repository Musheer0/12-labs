"use client";
import { Button } from "@/components/ui/button";

const GenerateButton = () => {
  return (
    <Button className="lg:w-fit! lg:h-9!  h-fit! py-3 w-full!">
      Generate Audio
    </Button>
  );
};

export default GenerateButton;
